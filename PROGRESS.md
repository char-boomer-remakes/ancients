# PROGRESS

Session log and phase acceptance tracking for ANCIENTS. Read this first, then `DECISIONS.md`, then run `npm test`.

## Current phase: **Phase 1 — Core loop** (in progress)

## Phase 1 checklist

| Item | Status |
| --- | --- |
| `npm run dev` boots to title, pick a starter | BUILDING |
| Kill and catch a kobold | BUILDING |
| Field caught creep as companion (entourage) | BUILDING |
| Buy and use Blink Dagger | BUILDING |
| Swap heroes mid-fight (1–5, swap cooldown, CD floor) | BUILDING |
| Manual save to a slot, reload, state intact | BUILDING |
| `npm test` green: data lint | BUILDING |
| `npm test` green: core boundary check (no three/DOM in core) | BUILDING |
| `npm test` green: synthetic-hero sim (JSON hero, every targeting type) | BUILDING |
| `npm test` green: fixed-seed 5v5 headless sim, stable winner | BUILDING |
| `npm test` green: capture + merge unit tests | BUILDING |
| 60-second demo script below verified | BUILDING |

## 60-second demo script

(to be filled in when Phase 1 passes)

## Session log

- **2026-06-12** — Project bootstrapped: Vite + TypeScript + three + vitest, repo initialized. Spec copied to `SPEC.md`. Beginning core sim.
